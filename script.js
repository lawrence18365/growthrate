document.addEventListener('DOMContentLoaded', () => {
    const pricingButton = document.querySelector('button');
    pricingButton.addEventListener('click', () => {
        alert('Pricing information coming soon!');
    });
});
